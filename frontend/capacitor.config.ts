import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'date.qvqw.classscore',
  appName: '班级积分管理系统',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
